<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1>Resturants 🍽️ </h1>
      <br>


      <br><br>

      <ul class="list-group mb-3">
        <% @restaurant.each do |item| %>
          <li class="list-group-item">
            <%= link_to item.name, restaurant_path(item) %>
          </li>
          <%# <p><%= item.address %>
          <%# <p><%= item.phone_number %>
          <%# <p><%= item.category %>
        <% end %>
      </ul>
        <%= link_to "New Restaurent 🆕", new_restaurant_path, class: 'btn btn-primary' %>
    </div>
  </div>
</div>
